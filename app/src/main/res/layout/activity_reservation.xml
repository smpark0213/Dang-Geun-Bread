<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ReservationActivity">

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/store_reservaton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="예약할 가게 이름"
        android:textSize="32sp"
        />

    <!-- 여태 장바구니에 넣었던 빵 리스트 -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/reservation_bread_recycle"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/store_reservaton"
        app:layout_constraintBottom_toTopOf="@id/btn_pick_up_time"/>

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btn_pick_up_time"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toTopOf="@id/btn_real_reservation"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:text="픽업 시간 선택"
        />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btn_real_reservation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:text="예약하기"/>

</androidx.constraintlayout.widget.ConstraintLayout>